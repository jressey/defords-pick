<% if game.present? %>
  <div class="card hot-game-card">
    <div class="card-block">
      <%= render partial: "shared/game", locals: { game: game } %>
    </div>
  </div>
  <%= render partial: "shared/stream_link", locals: { sport_name: game.sport.name}%>
<% end %>